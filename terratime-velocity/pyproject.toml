[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "terratime"
version = "0.1.0"
description = "TerraTime Pillar 1 — Growth Velocity & Acceleration Engine"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
